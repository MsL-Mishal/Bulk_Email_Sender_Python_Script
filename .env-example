# Email Credentials (Replace with actual credentials in .env, but NOT in this example file)
SENDER_EMAIL=your_email@gmail.com
APP_PASSWORD=your_app_password

# File Paths (Optional: Add if you have a specific path for attachments)
ATTACHMENTS_FOLDER=attachments/

# Log File Path
LOG_FILE=logs/email_log.txt

# SMTP Configuration
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# For Outlook, the SMTP_SERVER value should be `smtp.office365.com` and SMTP_PORT is 587 itself

# Different email providers have different SMTP server details. Here are some common ones:

<!-- 
`Email Provider`	`SMTP Server`	`SMTP Port`

Gmail	smtp.gmail.com	587 (TLS)
Outlook/Hotmail	smtp.office365.com	587 (TLS)
Yahoo Mail	smtp.mail.yahoo.com	465 (SSL)
Zoho Mail	smtp.zoho.com	587 (TLS)
iCloud Mail	smtp.mail.me.com	587 (TLS)

For other providers, check their official documentation by searching "SMTP settings for [your email provider]" on Google. -->